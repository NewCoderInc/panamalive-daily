`sample-events.json` is **test data**, not a real agenda. It exists so
`run_local.sh` and the renderer can be exercised without a weekly build.

The real `events.json` and `tx.json` come from the `pty-week` pass and belong
in the repo root. Do not post from this file.
